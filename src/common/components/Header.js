import React from "react";
import { styled } from "@mui/system";

const Header = styled("header")({ border: "1px solid red" });

export default () => (
  <Header>
    <h1>MarketPlaceHolder</h1>
  </Header>
);
